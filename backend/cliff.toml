[changelog]
header = ""
body = """
{% for group, commits in commits | group_by(attribute="group") %}
### {{ group | upper_first }}
{% for commit in commits %}
- {{ commit.message | upper_first }} ([{{ commit.id | truncate(length=7, end="") }}]({{ "https://github.com/PowerOfCreation/SimpleShoppingListApp/commit/" ~ commit.id }}))
{% endfor %}
{% endfor %}\
"""
footer = ""
trim = true

[git]
conventional_commits = true
filter_unconventional = false
protect_breaking_commits = true
tag_pattern = "^backend-v[0-9]+\\.[0-9]+\\.[0-9]+(-rc\\.[1-9][0-9]*)?$"
# RC tags must still match tag_pattern (needed so the changelog step's
# --tag override for an RC is accepted instead of falling back to the
# last real tag), but must never be a bump/changelog boundary themselves.
ignore_tags = "-rc\\.[1-9][0-9]*$"
include_paths = ["backend/**"]
commit_parsers = [
  { message = "^feat", group = "Features" },
  { message = "^fix", group = "Bug Fixes" },
  { message = "^perf", group = "Performance" },
  { message = "^refactor", group = "Refactoring" },
  { message = "^ci", group = "CI" },
  { message = "^docs", group = "Documentation" },
  { message = "^test", group = "Testing" },
  { message = "^chore\\(deps\\)", group = "Dependencies" },
  { message = ".*", group = "Other" },
]

[bump]
initial_tag = "backend-v0.1.0"
features_always_bump_minor = true
breaking_always_bump_major = true
