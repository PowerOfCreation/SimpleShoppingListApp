Backend deployed as {{ include "backend.fullname" . }} in namespace {{ .Release.Namespace }}.

To check it's reachable:
  kubectl port-forward svc/{{ include "backend.fullname" . }} 8080:{{ .Values.service.port }} -n {{ .Release.Namespace }}
  curl http://localhost:8080/healthz

Run the built-in connectivity test:
  helm test {{ .Release.Name }} -n {{ .Release.Namespace }}

Notes:
{{- if not .Values.serviceMonitor.enabled }}
  - serviceMonitor.enabled=false (default). Set it to scrape the backend's
    /metrics endpoint.
{{- end }}
  - preStopSleepSeconds=0 (default). See README.md "Known limitations" for
    how it relates to the app's own graceful shutdown on SIGTERM.
