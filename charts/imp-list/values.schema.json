{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "imp-list",
  "type": "object",
  "required": ["backend"],
  "properties": {
    "ingress": {
      "type": "object",
      "properties": {
        "enabled": { "type": "boolean" },
        "host": { "type": "string" },
        "tls": {
          "type": "object",
          "properties": {
            "enabled": { "type": "boolean" },
            "secretName": { "type": "string" }
          },
          "if": {
            "properties": { "enabled": { "const": true } }
          },
          "then": {
            "properties": { "secretName": { "type": "string", "minLength": 1 } },
            "required": ["secretName"]
          }
        }
      },
      "if": {
        "properties": { "enabled": { "const": true } }
      },
      "then": {
        "properties": { "host": { "type": "string", "minLength": 1 } },
        "required": ["host"]
      }
    },
    "backend": {
      "type": "object",
      "required": ["image", "service", "secret"],
      "properties": {
        "replicaCount": { "type": "integer", "minimum": 0 },
        "preStopSleepSeconds": { "type": "integer", "minimum": 0 },
        "image": {
          "type": "object",
          "required": ["repository", "tag"],
          "properties": {
            "repository": { "type": "string", "minLength": 1 },
            "tag": { "type": "string", "minLength": 1 },
            "pullPolicy": {
              "type": "string",
              "enum": ["Always", "IfNotPresent", "Never"]
            }
          }
        },
        "service": {
          "type": "object",
          "required": ["port", "targetPort"],
          "properties": {
            "type": { "type": "string" },
            "port": { "type": "integer", "minimum": 1, "maximum": 65535 },
            "targetPort": { "type": "integer", "minimum": 1, "maximum": 65535 }
          }
        },
        "ingress": {
          "type": "object",
          "properties": {
            "path": { "type": "string", "minLength": 1 },
            "pathType": { "type": "string" }
          }
        },
        "serviceMonitor": {
          "type": "object",
          "properties": {
            "enabled": { "type": "boolean" }
          }
        },
        "secret": {
          "type": "object",
          "properties": {
            "existingSecret": { "type": "string" },
            "existingSecretKeys": {
              "type": "object",
              "properties": {
                "databaseUrl": { "type": "string", "pattern": "^[-._a-zA-Z0-9]+$" },
                "keycloakIssuer": { "type": "string", "pattern": "^[-._a-zA-Z0-9]+$" },
                "keycloakClientId": { "type": "string", "pattern": "^[-._a-zA-Z0-9]+$" }
              }
            },
            "databaseUrl": { "type": "string" },
            "keycloakIssuer": { "type": "string" },
            "keycloakClientId": { "type": "string" }
          },
          "oneOf": [
            {
              "properties": { "existingSecret": { "type": "string", "minLength": 1 } },
              "required": ["existingSecret"]
            },
            {
              "properties": {
                "existingSecret": { "type": "string", "maxLength": 0 },
                "databaseUrl": { "type": "string", "minLength": 1 },
                "keycloakIssuer": { "type": "string", "minLength": 1 },
                "keycloakClientId": { "type": "string", "minLength": 1 }
              },
              "required": ["databaseUrl", "keycloakIssuer", "keycloakClientId"]
            }
          ]
        }
      }
    }
  }
}
